# Google Play Store Data Analytics

Main Python entry point: `app.py`

Input datasets:
- Play Store Data.csv
- User Reviews.csv

The application contains the six internship tasks and their assigned IST time windows.

For testing charts outside their time windows, temporarily set `DEBUG_MODE = True` in `app.py`.
For final behavior, keep `DEBUG_MODE = False`.
